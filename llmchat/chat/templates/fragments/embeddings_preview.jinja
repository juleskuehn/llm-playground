<div class="row">
  <div class="col-2">Document mean:</div>
  <div class="col">
    {{ doc.mean_embedding[:5] }} ...
  </div>
</div>
{% for chunk in doc.chunks.all().order_by("chunk_number")[:3] %}
<div class="row">
  <div class="col-2">Chunk {{ loop.index }}:</div>
  <div class="col">
    {{ chunk.embedding[:5] }} ...
  </div>
</div>
{% endfor %}
<p>{{ doc.chunks.count() }} chunks total.</p>
